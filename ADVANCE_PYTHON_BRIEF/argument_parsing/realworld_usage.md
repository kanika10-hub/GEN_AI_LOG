# Real World Usage

## Git

git commit -m "message"

Uses argument parsing.

---

## Docker

docker run nginx

Uses argument parsing.

---

## Claude Code

claude --help

Uses argument parsing.

---

## MCP Servers

python server.py --port 8000

Uses argument parsing.

---

## AI Applications

python rag.py --pdf notes.pdf

Uses argument parsing.

---

## Automation Scripts

python backup.py --folder Documents

Uses argument parsing.
