Definition

Encapsulation is the OOP principle of binding data and methods into a single unit (class) while restricting direct access to internal data using private attributes and controlled access methods such as getters, setters, or properties.

One important thing: in Python, encapsulation is mostly based on convention and name mangling (__variable) rather than strict access restrictions like in Java or C++. This distinction is often asked in interviews.
